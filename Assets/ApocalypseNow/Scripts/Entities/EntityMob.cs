using UnityEngine;
using System.Collections;

/// <summary>
/// Basisklasse für alle Entities, die nicht vom Spieler gesteuert werden. (z.B. Gegner, Questgeber)
/// </summary>
public class EntityMob : EntityLiving {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
